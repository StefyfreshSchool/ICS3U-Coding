package Week1;

/**
 * Assignment / Variables and Primitives
 */
public class ExampleFive {
    public static void main(String[] args) {
        int x = 80, y, z; //declare variables on same line

        y = 75;
        z = 87;

        double average = (x + y + z) / 3; //integer math if both operands are integers, doube math if any operand is double
        
        System.out.println(average);
    }
    
}
